//This is a very simplified version of the Potion() constructor you wrote earlier. The mock doesn't need to incorporate any random logic; it just needs to return valid values that the Player object can later use.
module.exports = function () {
    this.name = 'health';
    this.value = 20;
}